
{% if app.user %}
    <nav class="navbar navbar-expand-lg navbar-light">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"><i class='bx bx-menu'></i></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a href="{{path('account_index')}}" class="dropdown-item"> Mon compte </a>

                </li>
                <li class="nav-item">
                    <a href="{{path('account_bookings')}}" class="dropdown-item">Mes réservations</a>

                </li>
                <li class="nav-item">
                    <a href="{{path('account_profile')}}" class="dropdown-item"> Mon profile </a>

                </li>
                <li class="nav-item">
                    <a href="{{path('account_logout')}}" class="dropdown-item"> Deconnexion </a>

                </li>

            </ul>
        </div>
        <div style="float: right;">
            {{app.user.firstName}}
            <a href="{{path('account_profile')}}">
                <img style="width:20%;height:20%;" src="{{app.user.picture}}" class="avatar avatar-mini">
                
            </a>
        </div>
    </nav>

{% else %}
    <ul style="list-style: none;">
        <li class="nav-item">
            <a href="{{ path('account_register') }}" class="nav-link">Inscription</a>
        </li>
        <li class="nav-item">
            <a href="{{path('account_login')}}" class="nav-link">Connexion</a>
        </li>

    </ul>


{% endif %}



{#
{% for label,messages in app.flashes %}
            <div class="container">
                <div class="alert alert-{{label}}">
                    {% for message in messages %}
                        <p>{{message|raw}}
                    {% endfor %}
                </div>
            </div>
        {% endfor %}
        
        {% block body %}{% endblock %}



#}
        